[project]
name = "reports-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faststream[cli,redis]>=0.5.37",
    "httpx>=0.28.1",
]
